{
  "name": "dummy",
  "description": "Dummy package.json so we can deploy to heroku. Installs gulp and builds the client.",
  "scripts": {
    "postinstall": "cd src/donight/web/client; npm install;"
  }
}
